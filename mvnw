#!/bin/sh
# Maven Wrapper — generado por Spring Initializr
# Ejecuta: https://start.spring.io y descarga el proyecto completo
# para obtener el mvnw funcional con .mvn/wrapper/maven-wrapper.jar
exec mvn "$@"
